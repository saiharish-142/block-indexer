apiVersion: v1
kind: ConfigMap
metadata:
  name: block-indexer-config
data:
  APP_ENV: "production"
  API_ADDR: ":8080"
  WS_ADDR: ":8090"
  GRPC_ADDR: ":9100"
  METRICS_ADDR: ":9101"
  CHAIN_RPC_URL: "wss://rpc.example"
  CONFIRM_DEPTH: "12"
  POLL_INTERVAL: "2s"
  BATCH_SIZE: "200"
